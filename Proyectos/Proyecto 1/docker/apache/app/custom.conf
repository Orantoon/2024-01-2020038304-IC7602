# Archivo de configuración personalizado para Apache

# Definir el formato del registro
LogFormat "%h %l %u %t \"%r\" %>s %b" custom_format

# Definir el archivo de registro y el formato
CustomLog "/usr/local/apache2/logs/access_log" custom_format
